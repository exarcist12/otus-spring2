spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: library_test

mongock:
  enabled: false  # ← Отключаем Mongock для тестов

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2